# Day 2 — Readiness Assessment Test (RAT)

*Closed notes. Individual. ~10 minutes.*

---

## Questions

**1.** What Python function loads a CSV file into a DataFrame?

**2.** What Streamlit function displays a DataFrame as an interactive table?

**3.** What command do you type in the terminal to run a Streamlit app called `app.py`?

**4.** What does the following code do?

```python
df = df[df["Category"] == "Protein"]
```

**5.** Name one difference between `st.dataframe()` and `st.table()`.

**6.** What is the purpose of `st.sidebar`?

**7.** True or False: A Streamlit app re-runs the entire script from top to bottom every time the user interacts with a widget.

**8.** What is a GitHub repository?

**9.** In Pandas, if `df` has a column called `Retail Price`, how would you display only rows where `Retail Price` is greater than 30?

**10.** What does `df["Category"].unique()` return?

---

*After the RAT, move to [day2-exercise.md](day2-exercise.md).*
